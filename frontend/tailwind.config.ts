// tailwind.config.ts
import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: "class",            // use .dark mode class
  plugins: [],                   // you can add plugins here if needed
  // No need to define `theme` or `content` unless you have special needs
};

export default config;
